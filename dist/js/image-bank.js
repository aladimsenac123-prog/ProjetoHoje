const imageBank = { hero: [ { url: 'https: alt: 'Ciclista urbano com mochila de entrega', }, { url: 'https: alt: 'Bicicleta urbana em uma área verde', }, ], carga: [ { url: 'https: alt: 'Bicicleta de carga usada para entrega sustentável', }, { url: 'https: alt: 'Bicicleta estacionada em uma área verde', }, ], cidade: [ { url: 'https: alt: 'Bicicleta urbana em um ambiente ao ar livre', }, { url: 'https: alt: 'Bicicleta estacionada em uma área verde', }, ], }; const chooseImage = (images, slot) => { const index = Math.floor(Math.random() * images.length); const selected = images[index]; const fallback = slot.getAttribute('src'); slot.src = selected.url; slot.alt = selected.alt; slot.addEventListener('error', () => { slot.src = fallback; }, { once: true }); }; document.querySelectorAll('[data-image-bank]').forEach((slot) => { const images = imageBank[slot.dataset.imageBank]; if (images?.length) chooseImage(images, slot); });